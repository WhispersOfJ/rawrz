# ============================================================================
# The Bear Cave — Environment Template
# ============================================================================
# Copy this file to .env and fill in the values.
# All sensitive values should be stored in Docker secrets (see secrets/ directory)
# This file contains non-sensitive configuration and placeholder values.
#
# WARNING: Never commit .env to version control!
# ============================================================================

# ---- Identity / Runtime ----
PUID=1000
PGID=1000
TZ=America/New_York

# ---- Networking ----
HOST_IP=192.168.1.100  # Your server's IP address

# ---- Plex ----
PLEX_URL=http://HOST_IP:32400
PLEX_TOKEN=changeme
# PLEX_CLAIM is only needed on first-run registration of the Plex server
PLEX_CLAIM=

# ---- Service URLs (direct, no reverse proxy) ----
RADARR_URL=http://HOST_IP:7878
SONARR_URL=http://HOST_IP:8989
PROWLARR_URL=http://HOST_IP:9696
NZBDAV_URL=http://HOST_IP:3000
SEERR_URL=http://HOST_IP:5055

# ---- InfiniDysk (nzbdav) ----
# Shared SABnzbd-compatible API key used by Radarr/Sonarr and the NzbDAV UI.
FRONTEND_BACKEND_API_KEY=changeme
NZBDAV_WEBDAV_USER=usenet
# IMPORTANT: rclone.conf requires rclone-obfuscated passwords, not plaintext.
# Generate with: rclone obscure "your-password-here"
NZBDAV_WEBDAV_PASS=changeme
NZBDAV_RCLONE_RC_PASS=changeme
NZBDAV_PROFILE_TOKEN=changeme

# ---- Usenet Providers ----
NZBDAV_USENET_HOST=changeme
NZBDAV_USENET_PORT=563
NZBDAV_USENET_USER=changeme
NZBDAV_USENET_PASS=changeme

NZBDAV_USENET_BACKUP_HOST=changeme
NZBDAV_USENET_BACKUP_PORT=563
NZBDAV_USENET_BACKUP_USER=changeme
NZBDAV_USENET_BACKUP_PASS=changeme

# Eweka (tertiary provider slot) — re-adopted as the "eweka" nickname in the
# NZBDAV providers JSON in docker-compose.yml. These keys are REQUIRED: the
# compose environment-coverage gate fails when a ${VAR} has no template entry.
# Historical note: the slot was disabled 2026-09-06 after the provider rejected
# its credentials server-side (identical 502 Authentication Failed for the real
# login and probe logins); the former EWEEKA_* names are now TERTIARY_*.
NZBDAV_USENET_TERTIARY_HOST=changeme
NZBDAV_USENET_TERTIARY_PORT=563
NZBDAV_USENET_TERTIARY_USER=changeme
NZBDAV_USENET_TERTIARY_PASS=changeme

# ---- *arr API Keys ----
RADARR_API_KEY=changeme
SONARR_API_KEY=changeme
PROWLARR_API_KEY=changeme

# ---- Seerr (request management) ----
# API key generated in Seerr Settings -> API keys (config/seerr/settings.json).
# Used by stack-requests / stack-watchable (TODO.md #6) and
# scripts/arrival_notifier.py (TODO.md #7). Not auto-provisioned — paste the
# key from the Seerr UI.
SEERR_API_KEY=changeme

# ---- GitHub (release-please) ----
# Optional: classic PAT (repo scope) or fine-grained PAT (contents + pull-
# requests write) used by release-please to open the release PR. PRs opened
# with this token trigger validate.yml; PRs opened with the default
# GITHUB_TOKEN do not (GitHub recursion guard). GitHub Actions reads this
# ONLY from the repo Actions secret — run `./scripts/setup.sh` (or
# `--sync-github-secrets`) after changing it here to push the value to GitHub.
RELEASE_PLEASE_TOKEN=

# ---- Optional notifications ----
# Discord webhook for maintenance and re-adoption workflow alerts. Optional;
# workflows that reference DISCORD_WEBHOOK_URL skip alerts when it is unset.
DISCORD_WEBHOOK_URL=

# ============================================================================
# RAWRZ component configuration
# ============================================================================
# The stack keys above are the shared contract; the components below reuse them
# (PLEX_*, SONARR_*, RADARR_*, PROWLARR_*, SEERR_*, *_API_KEY) rather than
# redeclaring them. Component-only keys live here.

# ---- RAWRZ RPG (backend/rpg) ----
# ProbeConfig requires every key in this block plus the shared stack keys above.
# RPG_DB_URL is the RPG's own Postgres database — never point it at a scratch
# database outside a disposable proof run (see backend/rpg/tests/store_proof.rs).
RPG_DB_URL=postgresql://rpg:changeme@127.0.0.1:5432/movie_rpg
RPG_BIND_ADDRESS=0.0.0.0:46532
TMDB_API_KEY=changeme
TVDB_API_KEY=changeme
OMDB_API_KEY=changeme
FANART_API_KEY=changeme

# ---- RAWRZ Deck (backend/deck) ----
# Deck's own runtime settings; its stack keys come from the shared block above.
CAVE_DECK_PORT=7780
RUST_LOG=cave_deck=info,tower_http=info
CAVE_DECK_ENV_PATH=
CAVE_DECK_ENV_BACKUP_DIR=
CAVE_DECK_SMTP_USER=
CAVE_DECK_SMTP_PASS=
CAVE_DECK_SMTP_TO=
