# Cave Deck — runtime env (never commit real values; mirror of thebearcave's .env contract)
# Compose injects what the backend needs; local dev reads these directly.

# Backend
CAVE_DECK_PORT=7780
RUST_LOG=cave_deck=info,tower_http=info

# Stack API keys (read-only consumption at M0/M1; rotation lands in M2)
RADARR_API_KEY=
SONARR_API_KEY=
PROWLARR_API_KEY=
SEERR_API_KEY=
PLEX_TOKEN=
FRONTEND_BACKEND_API_KEY=

# Notifications (M6)
DISCORD_WEBHOOK_URL=
CAVE_DECK_SMTP_USER=
CAVE_DECK_SMTP_PASS=
CAVE_DECK_SMTP_TO=
