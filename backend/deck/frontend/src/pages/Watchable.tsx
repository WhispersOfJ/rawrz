import { makePlaceholder } from "./Placeholder";

export const FEATURE_IDS = ["lib.watchable"];

export default makePlaceholder({
  title: "Watchable",
  featureIds: FEATURE_IDS,
  milestone: "M5",
});
